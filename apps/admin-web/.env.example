# Local (non-Docker) development configuration for the admin dashboard.
# Copy to ".env.local" inside apps/admin-web/ when running `npm run dev` directly.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
